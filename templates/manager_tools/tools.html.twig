{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}
{% block stylesheets %}{% endblock %}
{% block body %}
    <p>Manager Name : {{ managerName.firstname }} {{ managerName.lastname }}</p>
    <h1>Register agent</h1>

    {{ form_start(registrationForm) }}
    {{ form_row(registrationForm.firstname) }}
    {{ form_row(registrationForm.lastname) }}
    {{ form_row(registrationForm.email) }}
    {{ form_row(registrationForm.password) }}
    {{ form_row(registrationForm.question) }}

    <button name="one" class="btn">Register as Agent level 1</button>
    <button name="two" class="btn">Register as Agent level 2</button>
    {{ form_end(registrationForm) }}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

    <div class="example-wrapper">
        <p>
            <a href="{{ path('app_logout') }}">Logout</a>
        </p>
        <p>
            <a href="{{ path('manager') }}">Manager Main Page</a>
        </p>
    </div>
{% endblock %}
